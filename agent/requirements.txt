# CyberPaw Agent — Python dependencies
#
# Install with:  pip install -r requirements.txt
#
# Optional heavy deps (install only what you need):
#   llama.cpp backend:  pip install llama-cpp-python
#   AirLLM backend:     pip install airllm transformers accelerate

# ── Core (always required) ─────────────────────────────────────────────────────
# No external deps for the core harness — uses only stdlib (asyncio, json, re, etc.)

# ── LLM backends (at least one required) ──────────────────────────────────────
# llama.cpp (recommended for most machines):
#   pip install llama-cpp-python
#   For GPU acceleration on macOS (Metal):
#   CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python --force-reinstall --no-cache-dir

# AirLLM (for machines with <8GB RAM):
#   pip install airllm transformers accelerate

# ── Memory probing (optional but recommended for auto backend selection) ───────
psutil>=5.9

# ── Web / Network tools ───────────────────────────────────────────────────────
httpx>=0.25.0
playwright>=1.40.0
